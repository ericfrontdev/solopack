# ==========================================
# VARIABLES OBLIGATOIRES POUR PRODUCTION
# ==========================================

# Base de données PostgreSQL (Neon recommandé)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# NextAuth v5 - Clé secrète (générez avec: openssl rand -base64 32)
AUTH_SECRET="your-secret-key-here-minimum-32-characters"
NEXTAUTH_SECRET="your-secret-key-here-minimum-32-characters"  # Même valeur que AUTH_SECRET

# Encryption key for sensitive data (Stripe keys, etc.)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be exactly 64 hex characters (32 bytes)
ENCRYPTION_KEY="your-64-character-hex-encryption-key-here"

# URL de votre application
# Développement: http://localhost:3000
# Production: https://votre-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# ==========================================
# VARIABLES OPTIONNELLES
# ==========================================

# Google OAuth (si vous voulez connexion Google)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Resend (pour envoyer des emails de factures)
RESEND_API_KEY="re_your_api_key_here"
EMAIL_FROM="noreply@votre-domaine.com"

# ==========================================
# PAIEMENTS EN LIGNE (OPTIONNEL)
# ==========================================

# Stripe Connect - Pour que chaque solopreneur reçoive les paiements directement
# IMPORTANT: Vous devez créer une application Stripe Connect sur https://dashboard.stripe.com/settings/applications
# Guide: https://stripe.com/docs/connect/enable-payment-acceptance-guide

# 1. Clé secrète Stripe (Dashboard > Developers > API keys)
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key"

# 2. Client ID pour OAuth (Dashboard > Settings > Connect > Client ID)
# Commence par ca_
STRIPE_CLIENT_ID="ca_your_client_id"

# 3. Secret du webhook (Dashboard > Developers > Webhooks)
# L'URL du webhook sera: https://votre-domaine.com/api/webhooks/stripe
# Événement à écouter: checkout.session.completed
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret"

# 4. Commission de la plateforme (optionnel, en pourcentage)
# Par exemple: 2 pour 2% de commission sur chaque transaction
# Laissez à 0 pour ne pas prendre de commission
STRIPE_APPLICATION_FEE_PERCENT="0"

# PayPal - Les utilisateurs configurent leur email PayPal dans leur profil
# Aucune configuration serveur n'est requise pour PayPal

# ==========================================
# PROGRAMME BETA
# ==========================================

# Date de fin de la période beta (format: YYYY-MM-DD)
# À définir une fois que tous les beta testeurs sont inscrits
# Exemple: BETA_END_DATE="2025-12-20"
BETA_END_DATE=""

# ==========================================
# CLOUDINARY - UPLOAD SCREENSHOTS
# ==========================================

# Pour le système de feedback - Upload de captures d'écran
# Créer un compte gratuit: https://cloudinary.com/users/register_free
# Documentation: voir CLOUDINARY_SETUP.md

# Cloud Name (visible publiquement, commence par NEXT_PUBLIC_)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your_cloud_name"

# API Key (depuis Dashboard Cloudinary)
CLOUDINARY_API_KEY="123456789012345"

# API Secret (PRIVÉ - ne jamais commit!)
CLOUDINARY_API_SECRET="your_api_secret_here"

# ==========================================
# RAPPELS AUTOMATIQUES - CRON JOB
# ==========================================

# Secret pour sécuriser l'endpoint du cron job de rappels
# Générez avec: openssl rand -base64 32
# L'endpoint /api/reminders/check nécessite ce secret
CRON_SECRET="your_cron_secret_here"
